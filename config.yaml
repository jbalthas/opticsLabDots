server:
  host: "localhost"
  port: 8000

lab:
  name: "USGS Camera and Imaging Systems Evaluation Laboratory"
  operator: "Jack"
  data_dir: "data"
  # IQ-Analyzer X hot folder — path configured inside IQ-Analyzer X (free version, path is locked)
  iqanalyzer_input_dir: "C:/WORK/HIF/Optics lab/Optics Lab Photos/Optics Lab Photos/Hot Folder"
  # Where IQ-Analyzer X writes result files — update this once confirmed (check Hot Folder > Jobs tab)
  iqanalyzer_output_dir: "C:/WORK/HIF/Optics lab/Optics Lab Photos/Optics Lab Photos/Hot Folder/Results"
  # Path to IQ-Analyzer X executable (for CLI/batch mode — check install location)
  iqanalyzer_exe: "C:/Program Files/Image Engineering/IQ-Analyzer-X/IQ-Analyzer-X.exe"
  # Path to MATLAB executable
  matlab_exe: "C:/Program Files/MATLAB/R2025a/bin/matlab.exe"
  matlab_scripts_dir: "C:/WORK/HIF/Optics lab/MatlabScripts"
  # Tag layout CSV for measure_distortion_tags (columns: id, x, y).
  # Generate once per chart from the IE TE251 tag coordinate sheet.
  # Leave blank to require the path to be supplied per-run in the GUI.
  distortion_layout_file: "C:/WORK/HIF/Optics lab/Charts/te251_layout.csv"

cameras:
  - id: "vivotek_it9388"
    name: "Vivotek IT9388"
    model: "IT9388"
    driver: "vivotek"
    ip: "192.168.13.101"
    port: 80
    username: "root"
    password: "REDACTED"
    notes: "Zone 4, Rail Stop 2 — primary QA camera (online)"

  - id: "vivotek_it9380h"
    name: "Vivotek IT9380-H"
    model: "IT9380-H"
    driver: "vivotek"
    ip: "192.168.13.100"
    port: 80
    username: "root"
    password: "REDACTED"
    notes: "Zone 4 — secondary camera (currently offline)"
